<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.google.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@android:drawable/sym_def_app_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.VangtiChai">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <!--
                Do NOT add android:configChanges="orientation|screenSize" here.
                The activity must be recreated on rotation so Android automatically
                inflates the correct alternate layout (layout-land, layout-sw600dp, etc.).
                State is preserved via onSaveInstanceState / onCreate(savedInstanceState).
                This exercises the activity lifecycle as required by the assignment.
            -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
